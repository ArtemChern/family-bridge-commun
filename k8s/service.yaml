apiVersion: v1
kind: Service
metadata:
  name: familybridge
  labels:
    app: familybridge
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: familybridge
